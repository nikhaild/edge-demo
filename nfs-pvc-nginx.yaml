---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-nginx2
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
  volumeName: pv-nginx1
  storageClassName: ""
---
apiVersion: v1
kind: Pod
metadata:
  name: nginx-nfs-example2
  namespace: default
spec:
  containers:
    - image: nginx
      name: nginx
      ports:
        - containerPort: 80
          protocol: TCP
      volumeMounts:
        - mountPath: /var/www
          name: pvc-nginx2
  volumes:
    - name: pvc-nginx2
      persistentVolumeClaim:
        claimName: pvc-nginx2
